# Danh mục Business Questions ứng viên

24 câu hỏi thuộc 5 mục tiêu trong [Business Objectives](business_objectives.md). Đây là **ứng viên phân tích**, chưa chốt KPI, grain kho dữ liệu hoặc quy tắc ETL. `Số dòng` luôn là số **bản ghi khoản vay được công bố**, không phải số khoản vay duy nhất. Mọi phép đo theo trạng thái là trạng thái tại `AsOfDate = 2026-06-30`.

**Feasibility:** `DIRECT` = tính từ thuộc tính nguồn; `DERIVED` = cần phép tính/nhóm dẫn xuất; `CONDITIONAL` = còn điều kiện nghiệp vụ, mẫu số hoặc thời gian quan sát phải chốt; `UNSUPPORTED` = thiếu dữ liệu tin cậy. `DQxx` tham chiếu [Data Quality Report](../data_understanding/data_quality_report.md). `FY` trong bảng là `ApprovalFY`; `State` là `ProjectState` trừ khi ghi khác. Những loại biểu đồ chỉ là gợi ý trình bày. CSV có **19 nhãn `ProcessingMethod`**, còn workbook liệt kê **18 cặp tên/mã**; một số nhãn viết khác (ví dụ `Working Capital CAPLine`, `Contract CAPLine`). Phân tích theo nhãn thô khả thi; đối chiếu tất cả nhãn sang mã chính thức cần xác minh riêng.

## G01 – Loan Approval

| BQ ID | Business Question | Business Value | Required Attributes | Suggested Measures | Analysis Dimensions | Feasibility và lý do | Data Quality Concerns | Interpretation Limitations | Suggested Visualization |
|---|---|---|---|---|---|---|---|---|---|
| BQ01 | Tổng vốn phê duyệt phân bố theo FY, quý tài chính và `ProjectState` ra sao? | Thấy quy mô, nhịp phê duyệt và vùng đóng góp. | `GrossApproval`, `ApprovalFY`, `ApprovalDate`, `ProjectState` | `SUM(GrossApproval)` | FY → quý → tháng; state | **DERIVED** vì quý/tháng tài chính phải suy từ `ApprovalDate`; FY/state là trực tiếp. | DQ15; ngày parse được đầy đủ. | Vốn phê duyệt, gồm cả `CANCLD` nếu không lọc; FY2026 chưa đủ năm. | Đường theo FY/quý, bản đồ hoặc heatmap state × FY |
| BQ02 | Số bản ghi công bố và quy mô vốn phê duyệt bình quân trên dòng khác nhau thế nào theo FY và `ProcessingMethod`? | Tách biến động số dòng khỏi biến động quy mô. | `GrossApproval`, `ApprovalFY`, `ProcessingMethod` | `COUNT(*)`; `SUM(GrossApproval)/COUNT(*)` | FY, method, state | **DERIVED** vì bình quân là tỷ số hai tổng. | DQ15; nhãn method phải đối chiếu workbook. | Không gọi count là số khoản vay duy nhất; trung bình là trên dòng. | Combo cột số dòng và đường quy mô bình quân |
| BQ03 | Tăng trưởng vốn phê duyệt giữa các FY đầy đủ, và cùng kỳ tới 30/06 của FY2026, thay đổi ra sao theo state? | Nhìn động lượng mà không đánh đồng năm chưa hoàn chỉnh. | `GrossApproval`, `ApprovalFY`, `ApprovalDate`, `ProjectState` | % thay đổi của `SUM(GrossApproval)` giữa các kỳ tương ứng | FY, tháng/quý tài chính, state | **CONDITIONAL**: chốt tập FY đầy đủ và cửa sổ cùng kỳ; FY2026 không so trực tiếp với tổng FY2025. | DQ15. | So sánh mô tả hồ sơ công bố; chênh lệch không tự chứng minh nguyên nhân kinh tế. | Đường tăng trưởng, small multiples theo state |
| BQ04 | Cơ cấu vốn và số dòng theo nhóm quy mô `GrossApproval` khác nhau thế nào giữa FY và `BusinessAge`? | Xem danh mục nghiêng về khoản phê duyệt nhỏ hay lớn. | `GrossApproval`, `ApprovalFY`, `BusinessAge` | Count, tổng vốn, tỷ trọng vốn từng nhóm | Nhóm quy mô dẫn xuất, FY, business age | **DERIVED**: cần ranh giới nhóm quy mô được công bố rõ. | DQ04 (`BusinessAge` thiếu); DQ15. | Không gán ngưỡng nhóm là phân loại chính thức của SBA; nhãn tuổi đời cần xác minh. | Cột chồng 100%, ma trận nhóm × FY |
| BQ05 | Có mùa vụ theo tháng phê duyệt hay không, và mô hình tháng khác nhau giữa các state? | Tìm thời điểm tập trung hồ sơ để đào sâu. | `ApprovalDate`, `GrossApproval`, `ProjectState` | Count, `SUM(GrossApproval)` theo tháng tài chính | Tháng tài chính, FY, state | **DERIVED**: tháng tài chính từ `ApprovalDate`. | DQ15. | Chỉ mô tả phân bố ngày phê duyệt; FY2026 thiếu quý cuối, không kết luận mùa vụ ổn định từ một chuỗi ngắn. | Heatmap tháng tài chính × FY |

## G02 – SBA Guarantee

| BQ ID | Business Question | Business Value | Required Attributes | Suggested Measures | Analysis Dimensions | Feasibility và lý do | Data Quality Concerns | Interpretation Limitations | Suggested Visualization |
|---|---|---|---|---|---|---|---|---|---|
| BQ06 | Tổng vốn SBA bảo lãnh khi phê duyệt biến động ra sao theo FY và `ProcessingMethod`? | Quan sát cơ cấu cam kết bảo lãnh theo phương thức. | `SBAGuaranteedApproval`, `ApprovalFY`, `ProcessingMethod` | `SUM(SBAGuaranteedApproval)` | FY, method, state | **DIRECT**. | DQ15; kiểm tra nhãn method theo workbook. | Không phải giá trị SBA đã chi trả; FY2026 chưa đầy đủ. | Cột chồng theo FY/method |
| BQ07 | Tỷ trọng vốn SBA bảo lãnh trên vốn phê duyệt khác nhau thế nào theo FY, state và NAICS? | So cường độ bảo lãnh giữa phân khúc. | `SBAGuaranteedApproval`, `GrossApproval`, `ApprovalFY`, `ProjectState`, `NaicsCode` | `SUM(SBAGuaranteedApproval)/SUM(GrossApproval)` | FY, state, NAICS | **DERIVED**: tỷ số của hai tổng cùng tập lọc. | DQ15; phiên bản NAICS cần xác minh nếu gộp ngành. | Không lấy bình quân tỷ lệ dòng; không suy ra rủi ro hay hiệu quả chính sách. | Heatmap state × NAICS, bảng có mẫu số |
| BQ08 | Phần vốn phê duyệt không được SBA bảo lãnh phân bố theo lender hiện tại và method ra sao? | Nhìn cơ cấu phần vốn ngoài bảo lãnh phê duyệt. | `GrossApproval`, `SBAGuaranteedApproval`, `LocationID`, `BankName`, `ProcessingMethod` | `SUM(GrossApproval)-SUM(SBAGuaranteedApproval)` | Lender hiện tại, method, FY | **DERIVED**: hiệu hai tổng. | DQ15; nhận diện lender ưu tiên `LocationID`. | Không phải dư nợ hay mức tổn thất mà lender sẽ gánh; `BankName` là lender hiện được gán. | Top N lender, treemap |
| BQ09 | Tỷ trọng bảo lãnh thay đổi thế nào theo nhóm quy mô vốn phê duyệt và method? | Khám phá sự khác biệt theo sản phẩm và quy mô. | `GrossApproval`, `SBAGuaranteedApproval`, `ProcessingMethod` | Tỷ số hai tổng theo nhóm; count | Nhóm quy mô dẫn xuất, method, FY | **DERIVED**: cần ngưỡng nhóm; tỷ lệ tính trên tổng. | DQ15; nhãn method. | Phân bố có thể phản ánh quy tắc chương trình; chưa suy ra tác động của method. | Heatmap nhóm quy mô × method |

## G03 – Geographic & Industry

| BQ ID | Business Question | Business Value | Required Attributes | Suggested Measures | Analysis Dimensions | Feasibility và lý do | Data Quality Concerns | Interpretation Limitations | Suggested Visualization |
|---|---|---|---|---|---|---|---|---|---|
| BQ10 | Vốn phê duyệt tập trung ở các `ProjectState` và `ProjectCounty` nào, và cơ cấu đó đổi theo FY ra sao? | Nhận diện tập trung địa lý của địa điểm dự án. | `GrossApproval`, `ProjectState`, `ProjectCounty`, `ApprovalFY` | Count, `SUM(GrossApproval)` | State → county, FY | **DIRECT** cho state/county hiện có. | DQ15; county phải đi cùng state để tránh tên trùng. | Không đại diện nơi người vay/lender; không chia bình quân dân số vì thiếu mẫu số. | Bản đồ state và bảng drill-down county |
| BQ11 | Tỷ trọng vốn phê duyệt của mỗi state trong tổng vốn cùng FY thay đổi thế nào? | Đo mức tập trung tương đối theo thời gian. | `GrossApproval`, `ProjectState`, `ApprovalFY` | Tổng state / tổng toàn bộ state trong FY | State, FY | **DERIVED**: cần giữ mẫu số toàn FY khi slice state. | DQ15. | FY2026 chỉ là cơ cấu cùng kỳ snapshot; không diễn giải là cả năm. | Cột chồng 100%, bump chart |
| BQ12 | Cơ cấu ngành NAICS theo state và FY khác nhau thế nào khi roll-up từ mã NAICS lên sector? | So cấu trúc ngành giữa địa phương. | `NaicsCode`, `NaicsDescription`, `ProjectState`, `ApprovalFY`, `GrossApproval` | Count, tổng vốn, tỷ trọng theo state/FY | NAICS code → sector, state, FY | **CONDITIONAL**: sector code có thể lấy 2 ký tự đầu; tên/rules cho ngành cần bảng NAICS đúng phiên bản. | DQ15; code có 6 chữ số nhưng phiên bản NAICS chưa xác nhận. | Không đồng nhất `NaicsDescription` với nhãn sector; chưa chắc mọi code còn hiệu lực. | Heatmap sector × state, drill-down NAICS |
| BQ13 | Sự khác nhau giữa bang người vay và bang dự án xuất hiện ở đâu và thay đổi ra sao theo FY? | Phân biệt địa điểm trụ sở với dự án. | `BorrState`, `ProjectState`, `ApprovalFY`, `GrossApproval` | Count, tổng vốn theo cặp bang | BorrState × ProjectState, FY | **DIRECT**: hai trường bang đều có trong CSV. | DQ15; không thấy mã bang ngoài tập kiểm tra hiện tại. | Không đo luồng tiền liên bang hoặc địa điểm giải ngân; địa chỉ người vay có thể thay đổi. | Ma trận cặp bang, flow map nếu cần |
| BQ14 | `JobsSupported` được lender báo cáo phân bố thế nào theo ngành NAICS và state dự án? | Mô tả số việc làm khai báo cùng phân bố vốn. | `JobsSupported`, `NaicsCode`, `ProjectState`, `GrossApproval`, `ApprovalFY` | `SUM(JobsSupported)`; vốn phê duyệt/việc làm khai báo khi mẫu số > 0 | NAICS, state, FY | **CONDITIONAL**: tính được số báo cáo nhưng phải chốt cách diễn giải và xử lý mẫu số 0. | DQ15; 45.739 dòng `JobsSupported=0`. | SBA không xác minh số việc làm; không gọi là số người duy nhất hay tác động nhân quả. | Scatter vốn vs jobs, bảng xếp hạng |

## G04 – Loan Portfolio Composition

| BQ ID | Business Question | Business Value | Required Attributes | Suggested Measures | Analysis Dimensions | Feasibility và lý do | Data Quality Concerns | Interpretation Limitations | Suggested Visualization |
|---|---|---|---|---|---|---|---|---|---|
| BQ15 | Danh mục vốn phê duyệt tập trung ở lender hiện được gán nào và mức tập trung đổi theo FY? | Nhìn phân bố theo đối tác đang giữ hồ sơ. | `LocationID`, `BankName`, `GrossApproval`, `ApprovalFY` | Tổng vốn, count, tỷ trọng Top N | Lender hiện tại, FY, state | **DERIVED**: tỷ trọng Top N cần tổng tham chiếu; tổng vốn trực tiếp. | DQ15; `BankName` có thể trùng/đổi, dùng `LocationID` để nhóm. | Không gọi là lender phê duyệt ban đầu; top lender không đồng nghĩa rủi ro tập trung hiện tại. | Top N bar + phần còn lại |
| BQ16 | Cơ cấu vốn và số dòng theo nhóm kỳ hạn thay đổi thế nào theo method và FY? | Nhìn độ dài cam kết theo sản phẩm. | `TermInMonths`, `GrossApproval`, `ProcessingMethod`, `ApprovalFY` | Count, tổng vốn, tỷ trọng nhóm kỳ hạn | Nhóm kỳ hạn dẫn xuất, method, FY | **DERIVED**: cần quy tắc nhóm tháng. | DQ07 (11 kỳ hạn 0), DQ15. | Kỳ hạn phê duyệt, không phải thời gian trả nợ thực tế; nhóm 0 cần giữ riêng/chờ xác minh. | Cột chồng 100% |
| BQ17 | Cơ cấu `BusinessType` và `BusinessAge` khác nhau thế nào theo FY và state? | Xem phân bố hồ sơ theo đặc điểm doanh nghiệp. | `BusinessType`, `BusinessAge`, `ApprovalFY`, `ProjectState`, `GrossApproval` | Count, tổng vốn | Business type × age, FY, state | **DIRECT**: nhãn nguồn đã có. | DQ03–DQ04, DQ15; `BusinessAge` có nhãn `Unanswered`. | Không gán nghĩa sâu cho nhãn tuổi đời chưa xác minh; để riêng nhóm thiếu. | Heatmap age × type, cột chồng |
| BQ18 | Lãi suất ban đầu khác nhau thế nào giữa `FixedorVariableInterestInd` và method, sau khi xét quy mô khoản vay? | Mô tả giá phê duyệt trong các phân khúc. | `InitialInterestRate`, `FixedorVariableInterestInd`, `ProcessingMethod`, `GrossApproval` | Trung bình/median lãi suất ban đầu; count quan sát | Rate type, method, nhóm quy mô | **CONDITIONAL**: chốt đơn vị lãi suất, xử lý giá trị 0/thiếu và thống kê trọng số nếu dùng. | DQ01–DQ02, DQ08 (87 số 0), DQ15. | Không phải lãi suất hiện hành hoặc mức giá do SBA đặt; không suy ra nguyên nhân chênh lệch. | Box plot/bảng phân vị |
| BQ19 | Tỷ trọng vốn phê duyệt có tài sản bảo đảm khác nhau theo method và FY ra sao? | Khám phá cơ cấu collateral theo hồ sơ. | `CollateralInd`, `GrossApproval`, `ProcessingMethod`, `ApprovalFY` | Vốn có cờ collateral / tổng vốn; count | Collateral flag, method, FY | **CONDITIONAL**: cần xác minh ý nghĩa mã Y/N trong CSV. | DQ15; workbook chỉ mô tả indicator, không giải mã nhãn. | Cờ do lender báo, không có giá trị tài sản hay mức bảo đảm; không suy ra rủi ro. | Cột chồng 100% |

## G05 – Loan Status & Outcomes

| BQ ID | Business Question | Business Value | Required Attributes | Suggested Measures | Analysis Dimensions | Feasibility và lý do | Data Quality Concerns | Interpretation Limitations | Suggested Visualization |
|---|---|---|---|---|---|---|---|---|---|
| BQ20 | Cơ cấu trạng thái của bản ghi công bố tại snapshot khác nhau thế nào theo FY và method? | Thấy danh mục đang ở trạng thái nào tại 30/06/2026. | `LoanStatus`, `ApprovalFY`, `ProcessingMethod`, `AsOfDate` | Count, tỷ trọng trạng thái trong từng cohort | FY, status, method | **DIRECT** cho nhãn thô; mapping `P I F` cần xác nhận nếu dùng mã PIF. | DQ11, DQ15. | Không phải lịch sử trạng thái tại mỗi FY; cohort có tuổi quan sát khác nhau. | Cột chồng 100% theo cohort |
| BQ21 | Tỷ trọng dòng `CHGOFF` đã quan sát tại snapshot khác nhau thế nào theo cohort FY, state và NAICS? | Tìm phân khúc có kết quả charge-off được ghi nhận nhiều hơn. | `LoanStatus`, `ApprovalFY`, `ProjectState`, `NaicsCode`, `AsOfDate` | Count CHGOFF / count mọi dòng của cùng cohort và lát cắt | FY, state, NAICS | **CONDITIONAL**: phải công bố mẫu số và thời gian quan sát; nếu so ngành sector cần mapping. | DQ15; DQ09/DQ13 nếu dùng ngày charge-off. | Chỉ là tỷ trọng tại snapshot, **không phải default rate cuối cùng** hay xác suất vỡ nợ. | Heatmap có count mẫu số, cohort chart |
| BQ22 | `GrossChargeOffAmount` được ghi nhận phân bố thế nào theo cohort FY, state và ngành? | Mô tả quy mô gross charge-off quan sát được. | `GrossChargeOffAmount`, `LoanStatus`, `ApprovalFY`, `ProjectState`, `NaicsCode` | `SUM(GrossChargeOffAmount)`; count CHGOFF | FY, state, NAICS | **CONDITIONAL**: cần chốt lọc trạng thái và có dùng `ChargeOffDate` hay không. | DQ09 (5 CHGOFF thiếu ngày), DQ13 (22 ngày sau snapshot), DQ15. | Gross amount gồm phần bảo lãnh và không bảo lãnh; không phải net loss SBA; cohort chưa trưởng thành. | Cột theo cohort, heatmap state × ngành |
| BQ23 | Tỷ trọng dòng `P I F` đã quan sát theo cohort FY và nhóm quy mô phê duyệt khác nhau thế nào? | So mức độ đã trả hết trong những nhóm phê duyệt. | `LoanStatus`, `ApprovalFY`, `GrossApproval` | Count `P I F` / count mọi dòng cùng cohort/nhóm | FY, nhóm quy mô, status | **CONDITIONAL**: cần mapping `P I F`/`PIF`, nhóm quy mô và mẫu số. | DQ11, DQ10/DQ12 nếu dùng ngày PIF, DQ15. | Không phải xác suất hoàn trả cuối cùng; cohort mới ít thời gian để PIF. | Cột cohort × nhóm quy mô |
| BQ24 | Trong tập hồ sơ đã có kết quả `P I F` hoặc `CHGOFF`, tỷ trọng `CHGOFF` khác nhau thế nào theo state và business age? | So cấu trúc **tập đã giải quyết**, tách khỏi hồ sơ còn mở. | `LoanStatus`, `ProjectState`, `BusinessAge`, `ApprovalFY` | Count CHGOFF / (count CHGOFF + count `P I F`) | State, business age, FY | **CONDITIONAL**: xác nhận mapping PIF, ngưỡng cỡ mẫu và định nghĩa tập resolved. | DQ04, DQ11, DQ15. | Tỷ lệ có điều kiện trên tập resolved, **không phải** tỷ lệ charge-off toàn danh mục hay ước lượng rủi ro. | Dot plot kèm count mẫu số |

## Những câu hỏi hiện không được hỗ trợ

Không xếp các câu sau vào 24 ứng viên khả thi vì CSV không có trường cần thiết: **dư nợ còn lại**, **giá trị đã giải ngân**, **lịch sử chuyển trạng thái**, **thu hồi sau charge-off**, **tổn thất ròng của SBA**, **xác suất vỡ nợ cuối cùng** và **tác động nhân quả của chương trình**. `FirstDisbursementDate` là ngày, không cung cấp số tiền giải ngân; `GrossChargeOffAmount` không chứa recovery.
